apiVersion: v1
kind: Service
metadata:
  name: agent-team
  namespace: ai-ops
  labels:
    app: agent-team
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  - name: metrics
    port: 8000
    targetPort: 8000
  selector:
    app: agent-team
